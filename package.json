{
  "name": "moodtracker-frontend-tooling",
  "private": true,
  "type": "commonjs",
  "description": "JS unit tests (src/main/resources/static/js) and Prettier formatting for JS/CSS. Not part of the Gradle build or the app runtime - the app itself ships plain <script>/<link> tags with no bundler.",
  "scripts": {
    "test": "vitest run",
    "format": "prettier --write \"src/main/resources/static/js/**/*.{js,mjs}\" \"src/main/resources/static/css/**/*.css\"",
    "format:check": "prettier --check \"src/main/resources/static/js/**/*.{js,mjs}\" \"src/main/resources/static/css/**/*.css\""
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
